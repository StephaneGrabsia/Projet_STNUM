---
title: "parser"
output: html_document
date: "2022-11-25"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
data <- read.csv("data_project.csv", sep=';')
```

```{r}
data_of_interest = data[c(6:8)]
print(data_of_interest)
```

```{r}
hist(as.numeric(data_of_interest$fat_100g) ,
     freq=FALSE,
     breaks=50,
     xlab="fat",
     main="Histogramme")

boxplot(as.numeric(data_of_interest$fat_100g),main="Boite à moustaches fat")

fat<-data[data$fat_100g>0,]$fat_100g
hist(as.numeric(fat) ,
     freq=FALSE,
     breaks=50,
     xlab="fat (with no 0)",
     main="Histogramme")
```

```{r}
hist(as.numeric(data_of_interest$saturated.fat_100g) ,
     freq=FALSE,
     breaks=50,
     xlab="saturated fat",
     main="Histogramme")

boxplot(as.numeric(data_of_interest$saturated.fat_100g),main="Boite à moustaches saturated fat")

saturated.fat<-data[data$saturated.fat_100g>0,]$saturated.fat_100g
hist(as.numeric(saturated.fat) ,
     freq=FALSE,
     breaks=50,
     xlab="saturated fat (with no 0)",
     main="Histogramme")
```


```{r}
hist(as.numeric(data_of_interest$fiber_100g) ,
     freq=FALSE,
     breaks=50,
     xlab="fiber",
     main="Histogramme")

boxplot(as.numeric(data_of_interest$fiber_100g),main="Boite à moustaches fiber")

fiber<-data[data$fiber_100g>0,]$fiber_100g
hist(as.numeric(fiber) ,
     freq=FALSE,
     breaks=50,
     xlab="fiber (with no 0)",
     main="Histogramme")
```
